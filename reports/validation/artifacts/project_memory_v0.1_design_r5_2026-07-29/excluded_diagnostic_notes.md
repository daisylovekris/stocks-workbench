# Excluded diagnostic notes

Failed CUN Pi is `evidence_disposition=excluded_diagnostic`, `benchmark_eligible=false`, `adr_eligible=false`; it is not an Agent record and does not create a fourth record_type.
