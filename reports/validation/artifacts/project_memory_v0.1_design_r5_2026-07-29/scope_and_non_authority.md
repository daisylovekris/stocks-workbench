# Non-authority boundary

Memory navigates evidence only; it cannot decide permissions, official facts, business state, trading, Git action, or review results. It grants no downstream authority, does not depend on `memory/manifest.json`, and never requires a file to contain its own future commit identity.
