# Canonical package algorithm r10

Paths are bundle-relative. Enumerate every review material except the two self-identity manifests, sort UTF-8 paths, hash bytes and canonical JSON files array with sort_keys=true/separators=(',',':'). No old package needed.
