# Fully expanded r10 finding mapping

Each Fable P2/P3 and Lucien r2-r8 finding is one row in r9 mapping retained here by sealed copy; r10 adds: `R10-decimal` P2 schema decimal reason `candidate_schema_identity_invalid` T26; `R10-safe-value` P2 forbidden nested action T27; `R10-semantics` P2 canonical candidate bytes T28; `R10-lock` P2 review lock function identity T29; `R10-package` P2 relative-root rebuild T30. All blocked permissions false.
