# r13 golden input-set identity

The canonical JSON uses only calendar identity, semantic-config SHA and daily facts/Daily Review/Phase-B-manifest/authoritative-Phase-C-manifest SHA values. The resolver for symbol 300274 is registered in semantic config as sungrow/reviews/sungrow_review_{trade_date}.md. Unregistered symbols fail closed; no path guessing is allowed.

The Phase C index appears only in phase_c_authority_derivation_ledger.json and runtime-manifest audit/revalidation evidence. It is not an input-set member.
