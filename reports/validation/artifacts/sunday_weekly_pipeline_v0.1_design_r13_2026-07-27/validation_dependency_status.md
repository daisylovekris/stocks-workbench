# Validation dependency status

`ajv-cli@5.0.0` was fetched through `npx` and used with `--spec=draft2020`. It validated the eleven complete decimal fixtures and the golden candidate against the r11 schema. The recorded results are in `decimal_schema_validation.md`; no handwritten validator was substituted.
