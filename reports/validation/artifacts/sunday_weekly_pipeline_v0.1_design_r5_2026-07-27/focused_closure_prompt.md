# Focused Closure Prompt r5

仅审 r5 bundle；调用任务在 bundle 外提供 package、review manifest、file manifest 三枚 SHA。核对 manifest 后审全部 files，特别是 rules/body/Fable/golden facts/inventory/semantic config/mapping/status/test/lock documents。

反证：Phase B/C canonical evidence 是否都有 raw SHA 并进入 input set；semantic config 是否覆盖五类语义并参加 manifest/live revalidation；四类阶段失败是否唯一映射；黄金样本 inventory 是否真实；是否只有 request-key orchestration lock 而 official facts locks 仍按顺序存在。输出 P1/P2/P3/FOCUSED_CLOSURE_READY 与精确缺口。
