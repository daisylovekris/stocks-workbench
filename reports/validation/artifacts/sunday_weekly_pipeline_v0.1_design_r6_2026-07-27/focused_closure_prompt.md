# Focused Closure Prompt r6

仅审 r6 bundle；任务从 bundle 外提供并核对三枚身份 SHA。审全部 manifest.files，特别是 semantic config、实现快照和 module map。反证：SWP 是否只从 review index 解析 Phase C，再从认证 Phase C manifest 解析 Phase B；rebuild 无 runner 是否阻断；config raw bytes 是否进 input set/revalidation；是否残留虚构 symbol-hash paths。输出 P1/P2/P3/FOCUSED_CLOSURE_READY。
