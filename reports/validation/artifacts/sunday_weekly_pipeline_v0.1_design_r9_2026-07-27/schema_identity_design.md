# Candidate schema identity binding r9

Safe-read candidate schema once; bytes, SHA and JSON parse arise from one FD. Compare raw SHA to semantic config `candidate_schema_raw_sha256` at initial snapshot and final commit. Mismatch is `blocked_identity_conflict:candidate_schema_identity_invalid`. Schema update must update config and produces a new input set.
